export const metadata = {
  title: "Contact Us – Dashify",
  description:
    "Have a project in mind? Reach out to the Dashify team for inquiries, support, or partnership opportunities. Let's build something extraordinary together.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
